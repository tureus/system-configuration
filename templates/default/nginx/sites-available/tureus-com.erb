server {
  listen   80;
  server_name  tureus.com, <%= node.ipaddress %>;

  access_log  <%= node['nginx']['log_dir'] %>/localhost.access.log;

  root <%= @root %>;

  location / {
    try_files $uri /index.html;
  }
}