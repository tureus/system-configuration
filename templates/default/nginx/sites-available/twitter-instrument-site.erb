server {
  listen   80;
  server_name illinois.tureus.com;

  access_log <%= node['nginx']['log_dir'] %>/twitter-instrument.access.log;

  root <%= @frontend_root %>;

  location /v1/ {
    proxy_pass http://localhost:8080/v1/;
  }

  location / {
    try_files $uri /index.html;
  }
}